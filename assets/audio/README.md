# 可选音频资源

此目录默认不附带音频，网页也默认静音。

预留文件：

```text
ambient-space.mp3  低沉、无明显旋律的循环环境音
flyby.mp3          镜头飞行的短促空间感音效
select.mp3         选择天体或控件的轻微反馈音
```

请只放入你拥有使用权的资源。加入 `ambient-space.mp3` 后，将 `main.js` 中 `audioController.installed` 改为 `true`。音频只会在用户主动点击声音按钮后播放。
